import styled from 'styled-components';

/**
 * Wrapper for control elements in the header.
 */
const HeaderIconsWrapper = styled('div')({
  position: 'absolute',
  top: '50%',
  right: '16px',
  transform: 'translateY(-50%)',
  padding: '0',
  lineHeight: '0',
});

export default HeaderIconsWrapper;
